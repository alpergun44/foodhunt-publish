{
  "name": "foodhunt",
  "version": "2.5.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "cd frontend && npm ci && npm run build && cd ../backend && npm ci",
    "start": "cd backend && node server.js"
  }
}
